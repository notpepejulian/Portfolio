---
import Layout from '../layouts/Layout.astro';
---

<style>
	html {
	  scroll-behavior: smooth;
	}
	
	@keyframes float {
		0%, 100% {
			transform: translateY(0px);
		}
		50% {
			transform: translateY(-20px);
		}
	}
	
	.float-animation {
		animation: float 3s ease-in-out infinite;
	}
	
	@keyframes fall {
		0% {
			top: -10%;
			opacity: 1;
		}
		100% {
			top: 110%;
			opacity: 0;
		}
	}
	
	.fall-animation {
		animation: fall 8s linear infinite;
		position: absolute;
	}
	
	@keyframes pulse-glow {
		0%, 100% {
			opacity: 1;
			box-shadow: 0 0 20px rgba(34, 211, 238, 0.3);
		}
		50% {
			opacity: 0.8;
			box-shadow: 0 0 40px rgba(34, 211, 238, 0.6);
		}
	}
	
	.pulse-glow {
		animation: pulse-glow 2s ease-in-out infinite;
	}
</style>

<Layout>
	<main class="flex flex-col min-h-screen items-center justify-center px-6 py-20 bg-transparent relative overflow-hidden">
		<div class="max-w-3xl w-full text-center z-10 relative">
            
			<!-- Icono de mantenimiento -->
			<div class="mb-8 flex justify-center">
				<div class="w-32 h-32 rounded-full bg-transparent flex items-center justify-center pulse-glow">
					<i class="fa-solid fa-gear text-6xl text-cyan-400" style="animation: spin 3s linear infinite;"></i>
				</div>
			</div>

			<h1 class="text-6xl sm:text-7xl font-black bg-gradient-to-r from-cyan-400 via-pink-400 to-yellow-300 bg-clip-text text-transparent mb-6 tracking-tight">
				¡Ups!
			</h1>
			
			<div class="mb-8">
				<p class="text-2xl sm:text-3xl font-semibold text-white/90 mb-4">
					Estamos en mantenimiento
				</p>
				<p class="text-xl sm:text-2xl font-medium text-white/70 mb-2">
					o terminando de ajustar ciertos cambios...
				</p>
			</div>

			<p class="text-lg text-gray-300 mb-10 max-w-2xl mx-auto leading-relaxed">
				Estoy trabajando en esta página... Vuelve pronto y mientras tanto, puedes navegar en los demás proyectos o secciones del sitio.
			</p>

			<div class="flex flex-col sm:flex-row gap-4 justify-center mb-8">
				<a href={`${import.meta.env.BASE_URL}`} class="inline-flex items-center justify-center px-8 py-4 rounded-full bg-gradient-to-r from-cyan-400 to-cyan-600
                hover:from-cyan-500 hover:to-cyan-700  text-white font-semibold shadow-lg transition-all duration-200 text-base group pulse-glow">
					<span>Volver al Inicio</span>
					<i class="fa-solid fa-arrow-right ml-2 group-hover:translate-x-1 transition-transform"></i>
				</a>
				<a href={`${import.meta.env.BASE_URL}proyectos/`} class="inline-flex items-center justify-center px-8 py-4 rounded-full bg-white/10 hover:bg-white/20 text-cyan-300 font-semibold shadow-lg transition-all duration-200 text-base border border-cyan-400/30">
					Ver Proyectos
				</a>
			</div>

			<!-- Información adicional -->
			<div class="mt-12 pt-8 border-t border-cyan-400/20">
				<p class="text-sm text-gray-400 mb-2">
					¿Necesitas contactarme?
				</p>
				<p class="text-cyan-300 text-sm">
					Puedes visitar otras secciones del portfolio mientras tanto
				</p>
			</div>
		</div>
	</main>
</Layout>
<style>
	@keyframes spin {
		from {
			transform: rotate(0deg);
		}
		to {
			transform: rotate(360deg);
		}
	}
</style>

